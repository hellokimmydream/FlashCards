@extends('layouts/main')

@section('title')Modifier deck — @end

@section('content')
  <h1>Modifier un deck</h1>

  <form method="POST" action="/decks/{{ deck.id }}" style="display:flex; flex-direction:column; gap:10px; max-width:420px;">
    @csrf
    <label>Titre</label>
    <input name="title" value="{{ deck.title }}" />

    <label>Description</label>
    <textarea name="description">{{ deck.description }}</textarea>

    <button type="submit">Enregistrer</button>
  </form>
@endsection