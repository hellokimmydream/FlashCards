@extends('layouts/main')

@section('title')Decks — @end

@section('content')
  <h1>Mes decks</h1>
  <a href="/decks/create">Nouveau deck</a>

  <ul style="margin-top:12px;">
    @each(deck in decks)
      <li style="margin:10px 0; padding:10px; border:1px solid #333;">
        <strong>{{ deck.title }}</strong>
        @if(deck.description)
          <p>{{ deck.description }}</p>
        @endif

        <a href="/decks/{{ deck.id }}/edit">Modifier</a>

        <form action="/decks/{{ deck.id }}/delete" method="POST" style="display:inline;">
          @csrf
          <button type="submit">Supprimer</button>
        </form>
      </li>
    @end
  </ul>
@endsection